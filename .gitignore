# Throwaway intermediates (not used by the script's computation)
_work/MSFT_daily.csv
_work/MSFT_yahoo.json

# Generated Schedule FA outputs
ScheduleFA_A3_*.csv
ScheduleFA_Dividends_*.csv
ScheduleFA_Report_*.html

# NOTE: _work/SBI_USD.csv and _work/MSFT_ohlc.csv ARE committed on purpose,
# as a versioned, auditable data snapshot. Refresh before each year's filing
# with:  .\Compute-ScheduleFA.ps1 -Year <YYYY> ... -RefreshPrices
